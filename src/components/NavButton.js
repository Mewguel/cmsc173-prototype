import { Button } from "grommet";

export const NavButton = (props) => (
    <Button
        margin={{ horizontal:'medium' }}
        color='#dadada'
        onMouseOver={() => {}}
        {...props}
    />
  );